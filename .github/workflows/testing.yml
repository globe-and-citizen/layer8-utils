name: test_all_files

on:
  workflow_dispatch:
  pull_request:
    types:
      - opened
    branches: 
      - development

jobs:
  Run_Unit_Tests:
    runs-on: ubuntu-latest
    steps: 
      - name: Install Go
        uses: actions/setup-go@v5
      - name: checkout
        uses: actions/checkout@4
      - name: "Run Unit Tests"
        run:  go test -v ./...

